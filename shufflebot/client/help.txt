
# Find the device
arduino-cli board list

# Install esp32 dep if not recognized
arduino-cli config init
arduino-cli core update-index
arduino-cli core install esp32:esp32

# NOTE to Connor: All of these may take 1-2 tries?
#   Not sure why, but 2nd try worked for a couple...

# Go into the client dir
cd /home/craigie/git/IndoorShufflebotSwarm/shufflebot/test/client

# Compile
arduino-cli compile --fqbn esp32:esp32:esp32 --build-path ./build .
# Upload
arduino-cli upload -p /dev/ttyUSB0 --fqbn esp32:esp32:esp32 --build-path ./build .
# Monitor Serial
arduino-cli monitor -p /dev/ttyUSB0 --config baudrate=115200

# Note for Connor - I put all this in a nice script buildandupload.sh


# WARNING - HOW TO ERASE FLASH
pip install esptool --break-system-packages
esptool.py -p /dev/ttyUSB0 erase_flash
